# Optional — enables Google Drive cloud sync.
# Create an OAuth Client ID (Web application) in Google Cloud Console and paste it here.
# See the "Cloud sync setup" section in README.md for step-by-step instructions.
# If left empty, the app works fine using localStorage only.
NEXT_PUBLIC_GOOGLE_CLIENT_ID=
