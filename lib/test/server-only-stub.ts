// Stub for the `server-only` package under vitest — see vitest.config.mts.
// The real one throws when imported outside a React Server Component, which is
// exactly the guard we want in the app and exactly what blocks a unit test.
export {};
